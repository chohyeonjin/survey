extends ../../layout

block content
  .container
    h1.page-header 설문
    
  
    table.table.table-bordered
      thead
        tr

          th 제목

          th
      tbody
        each question in questions
          tr

            td
              a(href='/surveys/#{question.id}')= question.title
              

            td= question.content
            td
              a.btn.btn-xs.btn-default(href='/surveys/#{question.id}/edit') 수정
              | 
              a.btn.btn-xs.btn-default(href='/surveys/#{survey.id}/#{question._id}/?_method=DELETE') 삭제
    
    a.btn.btn-primary(href='/surveys/#{survey.id}/questions/new') 질문추가
    | 
    a.btn.btn-default(href='/surveys') 목록          
    
