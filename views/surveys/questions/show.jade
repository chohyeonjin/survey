extends ../../layout

block content
  .container
  h3 질문목록
    h1.page-header= question.title
    p

    p.well(style='white-space:pre;')= question.content
    
    a.btn.btn-default(href='/surveys') 목록
