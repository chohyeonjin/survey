extends ../layout

block content
  .container
    h1.page-header 설문 목록
    
  
    table.table.table-bordered
      thead
        tr
          th 이메일
          th 제목
          th 도움말
          th 날짜
          th 조회수
          th
      tbody
        each survey in surveys
          tr
            td= survey.email
            td
              a(href='/surveys/#{survey.id}')= survey.title
            td= survey.content    
            td= moment(survey.createdAt).format('YYYY-MM-DD HH:mm:ss')
            td= survey.read
            if login.type == 'super'   
              td
                a.btn.btn-xs.btn-default(href='/surveys/#{survey.id}/edit') 수정
                | 
                a.btn.btn-xs.btn-default(href='/surveys/#{survey._id}?_method=DELETE') 삭제
              
    if login.type == 'super'   
      a.btn.btn-primary(href='/surveys/new') 글쓰기
    | 
    a.btn.btn-default(href='/') 처음화면          
    
